import Card from "./ui/Card.tsx";

// Keep the original filename as a lightweight wrapper so imports across the
// codebase don't need to change. This file previously contained the Card
// implementation; that has been moved to `components/ui/Card.tsx`.
export default Card;
